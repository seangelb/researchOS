# Local vehicle data

`vehicle.sqlite` initially contains one explicitly truncated real browser sample with
three listings, not a full inventory or a sales dataset. Its immutable extracted JSON
is retained under `raw/carvana/`. Both files are ignored by Git.

The synthetic learning fixture stays under `tests/fixtures/` and is not ingested here.
Additional captures require the explicit opt-in collector command in the vehicle guide.
